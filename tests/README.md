# Tests

Shared test guidance, fixtures, and repository-level test helpers will live
here when approved.

Module-specific tests should normally live beside their module under:

```text
modules/<module-name>/tests/
```

Tests must not include real tenant IDs, subscription IDs, regions, CIDRs,
resource names, credentials, state files, plan files, or environment-specific
configuration.

No tests are implemented yet.
