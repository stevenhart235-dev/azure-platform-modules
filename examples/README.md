# Examples

Repository-level examples and shared example guidance will live here when
needed.

Module-specific examples should normally live beside their module under:

```text
modules/<module-name>/examples/
```

Examples must use placeholder values only. They must not include real tenant
IDs, subscription IDs, regions, CIDRs, resource names, credentials, provider
configuration for real environments, or backend configuration.

No examples are implemented yet.
