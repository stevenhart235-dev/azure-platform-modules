# Basic Example

This example shows the smallest meaningful module call.

The values are placeholders. Deployment repositories must provide real
environment values through reviewed configuration.

This example is a standalone root module. Provider configuration will be added
when the Azure resource implementation begins so the example can be initialized
and validated independently. Backend blocks must never be added to examples.
