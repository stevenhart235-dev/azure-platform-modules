# Basic Example

This example shows the smallest meaningful module call.

The storage account ID is a placeholder. Deployment repositories must provide
the ID of an existing storage account created by a separate module or root
deployment.

This example is a standalone root module with provider configuration so it can
be initialized and validated independently.

Backend blocks must never be added to examples.
